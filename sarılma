--[=[
  SON VERSİYON - SADECE O SANA YAPIŞSIN
  - Hedef sana yapışır (sen ona değil)
  - Sen özgürce hareket edersin
  - O hiç hareket edemez
  - Çözünce olduğu yerde kalır
  - Animasyonlu geçişler
]=]

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local lp = Players.LocalPlayer

-- GUI
local gui = Instance.new("ScreenGui")
gui.Name = "YapisBana"
gui.Parent = lp:WaitForChild("PlayerGui")
gui.ResetOnSpawn = false

-- Ana pencere
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 280, 0, 200)
frame.Position = UDim2.new(0.5, -140, 0.5, -100)
frame.BackgroundColor3 = Color3.fromRGB(18, 18, 22)
frame.Active = true
frame.Draggable = true
frame.Parent = gui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 12)
corner.Parent = frame

local stroke = Instance.new("UIStroke")
stroke.Color = Color3.fromRGB(100, 150, 255)
stroke.Thickness = 1.5
stroke.Parent = frame

-- Başlık
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 45)
title.Text = "🎯 BANA YAPIŞ"
title.TextColor3 = Color3.fromRGB(220, 220, 255)
title.BackgroundTransparency = 1
title.Font = Enum.Font.GothamBold
title.TextSize = 18
title.Parent = frame

-- Hedef göstergesi
local targetFrame = Instance.new("Frame")
targetFrame.Size = UDim2.new(0.9, 0, 0, 40)
targetFrame.Position = UDim2.new(0.05, 0, 0.2, 0)
targetFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 32)
targetFrame.Parent = frame

local targetCorner = Instance.new("UICorner")
targetCorner.CornerRadius = UDim.new(0, 8)
targetCorner.Parent = targetFrame

local targetIcon = Instance.new("Frame")
targetIcon.Size = UDim2.new(0, 8, 0, 8)
targetIcon.Position = UDim2.new(0, 12, 0.5, -4)
targetIcon.BackgroundColor3 = Color3.fromRGB(100, 200, 255)
targetIcon.BorderSizePixel = 0
targetIcon.Parent = targetFrame

local targetIconCorner = Instance.new("UICorner")
targetIconCorner.CornerRadius = UDim.new(1, 0)
targetIconCorner.Parent = targetIcon

local targetName = Instance.new("TextLabel")
targetName.Size = UDim2.new(1, -30, 1, 0)
targetName.Position = UDim2.new(0, 25, 0, 0)
targetName.Text = "HEDEF: YOK"
targetName.TextColor3 = Color3.fromRGB(180, 180, 200)
targetName.BackgroundTransparency = 1
targetName.Font = Enum.Font.Gotham
targetName.TextSize = 13
targetName.TextXAlignment = Enum.TextXAlignment.Left
targetName.Parent = targetFrame

-- YAPIŞ butonu
local yapisBtn = Instance.new("TextButton")
yapisBtn.Size = UDim2.new(0.8, 0, 0, 45)
yapisBtn.Position = UDim2.new(0.1, 0, 0.4, 0)
yapisBtn.BackgroundColor3 = Color3.fromRGB(50, 100, 200)
yapisBtn.Text = "📌 BANA YAPIŞ"
yapisBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
yapisBtn.Font = Enum.Font.GothamBold
yapisBtn.TextSize = 15
yapisBtn.Parent = frame

local yapisCorner = Instance.new("UICorner")
yapisCorner.CornerRadius = UDim.new(0, 8)
yapisCorner.Parent = yapisBtn

-- ÇÖZ butonu
local cozBtn = Instance.new("TextButton")
cozBtn.Size = UDim2.new(0.8, 0, 0, 45)
cozBtn.Position = UDim2.new(0.1, 0, 0.6, 0)
cozBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 100)
cozBtn.Text = "🔓 ÇÖZ"
cozBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
cozBtn.Font = Enum.Font.GothamBold
cozBtn.TextSize = 15
cozBtn.Parent = frame

local cozCorner = Instance.new("UICorner")
cozCorner.CornerRadius = UDim.new(0, 8)
cozCorner.Parent = cozBtn

-- Kapatma
local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 26, 0, 26)
closeBtn.Position = UDim2.new(1, -34, 0, 8)
closeBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 48)
closeBtn.Text = "✕"
closeBtn.TextColor3 = Color3.fromRGB(200, 200, 220)
closeBtn.Font = Enum.Font.Gotham
closeBtn.TextSize = 14
closeBtn.Parent = frame

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(0, 6)
closeCorner.Parent = closeBtn

closeBtn.MouseButton1Click:Connect(function()
    if yapisti then coz() end
    gui:Destroy()
end)

-- Değişkenler
local yapisti = false
local hedef = nil
local weld = nil
local bp = nil
local bg = nil
local heartbeatConn = nil

-- En yakın oyuncuyu bul
local function findNearestPlayer()
    local char = lp.Character
    if not char then return nil end
    local root = char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("Torso")
    if not root then return nil end
    
    local pos = root.Position
    local nearest = nil
    local nearestDist = 15
    
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= lp then
            local pChar = player.Character
            if pChar and pChar.PrimaryPart then
                local dist = (pos - pChar.PrimaryPart.Position).Magnitude
                if dist < nearestDist then
                    nearestDist = dist
                    nearest = player
                end
            end
        end
    end
    return nearest
end

-- Yapıştır (SADECE O SANA YAPIŞIR)
local function yapis(target)
    local char = lp.Character
    local targetChar = target.Character
    
    if not char or not targetChar then return false end
    
    -- Senin root'un
    local myRoot = char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("Torso")
    -- Onun root'u
    local targetRoot = targetChar:FindFirstChild("HumanoidRootPart") or targetChar:FindFirstChild("Torso")
    -- Onun humanoid'i
    local targetHum = targetChar:FindFirstChildOfClass("Humanoid")
    
    if not myRoot or not targetRoot or not targetHum then return false end
    
    -- Hedefi tamamen durdur
    targetHum.PlatformStand = true
    targetHum.AutoRotate = false
    targetHum.WalkSpeed = 0
    targetHum.JumpPower = 0
    
    -- Fiziksel sabitleme
    targetRoot.Anchored = false
    targetRoot.CanCollide = false
    
    -- Eski bağlantıları temizle
    pcall(function() 
        if weld then weld:Destroy() end
        if bp then bp:Destroy() end
        if bg then bg:Destroy() end
        if heartbeatConn then heartbeatConn:Disconnect() end
    end)
    
    -- Weld (ONU SANA BAĞLA, SENİ ONA DEĞİL!)
    -- DİKKAT: Part0 = ONUN root'u, Part1 = SENİN root'un
    weld = Instance.new("Weld")
    weld.Part0 = targetRoot  -- ONUN root'u
    weld.Part1 = myRoot       -- SENİN root'un
    weld.C0 = CFrame.new(0, 0, 2)  -- Onun pozisyonu (önde dursun)
    weld.C1 = CFrame.new(0, 0, 0)  -- Senin pozisyonun
    weld.Parent = targetRoot
    
    -- Yumuşak animasyon
    local tween = TweenService:Create(weld, TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {C0 = CFrame.new(0, 0, 1.5)})
    tween:Play()
    
    -- BodyPosition (yedek)
    bp = Instance.new("BodyPosition")
    bp.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
    bp.P = 20000
    bp.Parent = targetRoot
    
    bg = Instance.new("BodyGyro")
    bg.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
    bg.P = 20000
    bg.Parent = targetRoot
    
    -- Heartbeat ile sürekli kontrol (SENİN pozisyonuna göre onu ayarla)
    heartbeatConn = RunService.Heartbeat:Connect(function()
        if not yapisti or not targetChar or not targetChar.Parent or not char or not char.Parent then
            return
        end
        if myRoot and targetRoot then
            -- Onu senin pozisyonuna göre ayarla (önde dursun)
            local targetPos = myRoot.Position + myRoot.CFrame.LookVector * 2 + Vector3.new(0, 0, 0)
            bp.Position = targetPos
            bg.CFrame = myRoot.CFrame
        end
    end)
    
    return true
end

-- Çöz (ışınlama YOK, olduğu yerde kalsın)
local function coz()
    yapisti = false
    
    if hedef and hedef.Character then
        local hum = hedef.Character:FindFirstChildOfClass("Humanoid")
        if hum then
            hum.PlatformStand = false
            hum.AutoRotate = true
            hum.WalkSpeed = 16
            hum.JumpPower = 50
        end
    end
    
    pcall(function()
        if weld then 
            -- Önce weld'i yumuşakça yok et
            local tween = TweenService:Create(weld, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {C0 = weld.C0 * CFrame.new(0, 0, 0.5)})
            tween:Play()
            tween.Completed:Connect(function()
                pcall(function() weld:Destroy() end)
            end)
        end
        if bp then bp:Destroy() end
        if bg then bg:Destroy() end
        if heartbeatConn then heartbeatConn:Disconnect() end
    end)
    
    weld = nil; bp = nil; bg = nil; heartbeatConn = nil
    hedef = nil
end

-- YAPIŞ butonu
yapisBtn.MouseButton1Click:Connect(function()
    if yapisti then
        targetName.Text = "ZATEN YAPIŞIK!"
        return
    end
    
    local target = findNearestPlayer()
    if target then
        targetName.Text = "⏳ YAPIŞIYOR..."
        task.wait(0.1)
        local success = yapis(target)
        if success then
            yapisti = true
            hedef = target
            targetName.Text = "📌 YAPIŞIK: " .. target.Name:upper()
            targetIcon.BackgroundColor3 = Color3.fromRGB(100, 255, 150)
            yapisBtn.BackgroundColor3 = Color3.fromRGB(70, 120, 220)
            yapisBtn.Text = "📌 YAPIŞIK"
        else
            targetName.Text = "❌ YAPIŞAMADI!"
        end
    else
        targetName.Text = "⚠️ YAKINDA KİMSE YOK!"
    end
end)

-- ÇÖZ butonu
cozBtn.MouseButton1Click:Connect(function()
    if yapisti then
        coz()
        targetName.Text = "HEDEF: YOK"
        targetIcon.BackgroundColor3 = Color3.fromRGB(100, 200, 255)
        yapisBtn.BackgroundColor3 = Color3.fromRGB(50, 100, 200)
        yapisBtn.Text = "📌 BANA YAPIŞ"
    else
        targetName.Text = "⚠️ YAPIŞIK KİMSE YOK!"
    end
end)

-- Karakter değişince
lp.CharacterAdded:Connect(function()
    coz()
    targetName.Text = "HEDEF: YOK"
    targetIcon.BackgroundColor3 = Color3.fromRGB(100, 200, 255)
    yapisBtn.BackgroundColor3 = Color3.fromRGB(50, 100, 200)
    yapisBtn.Text = "📌 BANA YAPIŞ"
end)

-- Bildirim
game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "Bana Yapış Sistemi",
    Text = "Sadece o sana yapışır, sen özgürsün!",
    Duration = 4
})
